export enum UrlPath {
  BASE = ' https://rslang-team54-learnwords.herokuapp.com',
  WORDS = 'words',
  USERS = 'users',
  SIGNIN = 'signin',
  TOKENS = 'tokens',
}

export enum Headers {
  TYPE = 'application/json',
}

export enum Methods {
  GET = 'GET',
  POST = 'POST',
  DELETE = 'DELETE',
  PUT = 'PUT',
}