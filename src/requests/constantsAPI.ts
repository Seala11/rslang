export enum UrlPath {
  base = ' https://rslang-team54-learnwords.herokuapp.com',
  words = 'words',
  users = 'users',
}

export enum Kkk {
  base = ' https://rslang-team54-learnwords.herokuapp.com',
  words = 'words',
  users = 'users',
}
