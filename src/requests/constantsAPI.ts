export enum UrlPath {
  base = ' https://rslang-team54-learnwords.herokuapp.com',
  words = 'words',
  users = 'users',
  signin = 'signin',
  tokens = 'tokens',
}

export enum Headers {
  content = 'application/json',
}

export enum Methods {
  get = 'GET',
  post = 'POST',
  delete = 'DELETE',
  pit = 'PUT',
}